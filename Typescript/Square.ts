class Square {
  public x: number;
  public y: number;
  public generation: number;
  constructor(x: number, y: number, generation: number) {
    this.x = x;
    this.y = y;
    this.generation = generation;
  }
}
